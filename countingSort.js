function countingSort (arr) {
    const maxVal = Math.max(...arr); // Find the maximum value in the array
    const count = new Array(maxVal + 1).fill(0); // Create a counting array with all elements set to 0
}